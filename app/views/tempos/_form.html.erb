<%= form_for(@tempo) do |f| %>
  <% if @tempo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tempo.errors.count, "error") %> prohibited this tempo from being saved:</h2>

      <ul>
      <% @tempo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bpm %><br>
    <%= f.number_field :bpm %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
